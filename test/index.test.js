'use strict';
/** TODO> Include some test for lib/index.js
 * For example:
 *  - process a demo.feature file and see if it's being loaded in the system
 *  - process a wrong demoDamanged.feature file and see if erros are being showed
 *  - check if multiple loading isn't adding the same file multiple times> teardown
 *  - what about if we forget to include any test framework> karma should show an error
 *  - what about if load two different files with the same content? the same feature's name? scenario's? step's name?
 */
